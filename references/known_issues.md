# Known Issues

| Date | Symptom | Root Cause | Fix | Prevention | Related File |
|---|---|---|---|---|---|
| 2026-07-17 | MATLAB reported an invalid expression before the wrapper could run | A dynamically generated `save(...)` command contained an extra quote after the path placeholder | Corrected the quoting in `sprintf('save(''%s'', %s);', ...)` | Parse-test dynamically generated MATLAB commands before an expensive solve | `scripts/run_livelink_batch.m` |
| 2026-07-17 | `mphsave` created the model, but MATLAB then failed to write `AppData\Roaming\MathWorks\MATLAB\R2022b\matlabprefs.mat` | The restricted process could not update the MATLAB preference directory; LiveLink and the model were not the cause | Ran through the approved MATLAB MCP environment with normal preference-directory access | Separate preference-permission errors from LiveLink/model failures and preserve the generated model before retrying | `src/test_livelink_connection.m`; `runs/20260717_100318/error_report.txt` |
| 2026-07-17 | `mphopen` said an Application Library model was a preview file | Some installed `.mph` files are download placeholders, not complete models | Selected a complete local official model that passed a read-only `mphopen` test | Verify every new official reference with read-only `mphopen` before inspecting its API | MCP inspection scripts; `capacitor_fringing_fields.mph` |
| 2026-07-17 | `cellstr(model.component.tags)` could not convert `java.lang.String[]` | COMSOL Java tag arrays are not directly accepted by MATLAB R2022b `cellstr` | Used `raw = cell(javaTags)` followed by `cellfun(@char, raw, 'UniformOutput', false)` | Route every COMSOL Java tag array through one conversion helper | `src/run_official_capacitor_fringing_baseline.m` |
| 2026-07-17 | A MATLAB MCP call supplied `args.CODEX_RUN_DIR`, but the project script could not see the variable | This MATLAB MCP implementation launches independent `matlab -batch` sessions and did not inject the critical argument | Generated a launcher that explicitly assigned `CODEX_RUN_DIR` and then ran the project script | Put every critical path/value in the generated launcher; do not rely on MCP `args` or a previous workspace | MCP launcher scripts; `src/run_official_capacitor_fringing_baseline.m` |
| 2026-07-17 | A completed 10-case sweep failed only while assembling its Markdown report with `horzcat` | A column cell array was concatenated with a row cell array | Used vertical column-cell concatenation and rebuilt the report from the saved model/workspace without resolving again | Keep report lines as N-by-1 cells and test report assembly with small data before a costly solve | `src/run_official_capacitor_fringing_sweep.m`; `runs/20260717_105341/error_report.txt` |
| 2026-07-17 | `mphselectbox(..., 'domain')` returned zero for a visible magnet domain | The query box was strictly inside the solid; COMSOL 5.6 box selection tests entity vertices | Expanded the box beyond the complete magnet bounding box with a small tolerance | Enclose the target entity's vertices, print returned IDs, and assert the expected count before assigning physics | `src/build_acdc_permanent_magnet.m`; `runs/20260717_111424_acdc_permanent_magnet/error_report.txt` |
| 2026-07-17 | The magnetostatic solver reported undefined material property `mur` in `mfc1` | The model had no material node, while the default feature still used `mur_mat=from_mat` | After checking allowed values in the official COMSOL 5.6 model, set `mur_mat=userdef` and `mur=1` explicitly | In material-free minimal models, inspect every `*_mat` source and supply all required properties | `src/build_acdc_permanent_magnet.m`; `runs/20260717_111853_acdc_permanent_magnet_retry1/error_report.txt` |
| 2026-07-17 | A solved COMSOL 5.6 structural model failed in `mphmax` with `Unknown property name: unit` | LiveLink 5.6 `mphmax` does not accept the `'unit', value` option supported by some other evaluators | Removed the option and used the expression's native unit; retained the supported unit option in `mphinterp` | Check installed-version help for each evaluator separately; do not assume option parity | `src/build_structural_cantilever.m`; `runs/20260717_113055_structural_cantilever/error_report.txt` |
| 2026-07-17 | `mphlaunch(model)` returned, but no Desktop remained and `modelsUsedByOtherClients()` stayed empty | MATLAB MCP batch sessions omitted `windir`; COMSOL 5.6 `comsolmphclient.exe` then crashed in WPF font initialization with `System.UriFormatException` | Before `mphlaunch`, set the missing process-level `windir` from the validated `SystemRoot`; keep the repair local to MATLAB and its child process | Never treat `mphlaunch` return as success; validate the exact client model tag, inspect Windows Application events on silent exits, and use the helper's process-only environment repair | `scripts/launch_comsol_visual_client.m`; `runs/20260717_145539_visual_sync_test/error_report.txt`; Windows Event IDs 1026/1000 |
| 2026-07-17 | Fresh-session visual verification found the right Desktop tag and parameter but failed an exact label assertion | `mphsave(model, outputPath)` changed the live model label from the pre-save test label to the output filename | Verify the intended pre-save label before saving, then expect the post-save label to equal the saved filename; use the parameter and exact server tag as persistent sync evidence | Capture label both before and after `mphsave`; do not assume a custom label survives save unchanged | `scripts/test_comsol_visual_sync.m`; `runs/20260717_150639_visual_sync_test/run_report.md` |
| 2026-07-17 | The visual test diary reported success and Desktop stayed responsive, but the MATLAB MCP tool call timed out after 300 seconds | The independent batch MATLAB process exited, while the launched visual client retained inherited output handles so the MCP transport did not see the command stream close | Left the Desktop running and verified completion from the diary, responsive `comsolmphclient.exe`, exact other-client model tag, and fresh MCP parameter readback | Never force-close the Desktop to clear this transport timeout; treat the four independent checks as the visual-mode completion contract | `runs/20260717_150639_visual_sync_test/matlab_diary.txt`; `fresh_mcp_verification.txt` |
| 2026-07-17 | The published bilingual operator guide showed mojibake while the project skill source displayed correct Chinese | A PowerShell text read reconstructed a UTF-8 file through the active legacy code page | Replaced the derived file with a byte-preserving copy of the verified UTF-8 source and added an explicit UTF-8 decode check | Use `Copy-Item` for exact document copies; when reading text use explicit UTF-8 and reject replacement characters or common mojibake markers before publishing | `outputs/comsol_visual_operator_guide_bilingual.md`; `references/operator_guide_bilingual.md` |
| 2026-07-17 | A before/after snapshot reported an unrelated result change `touchpostshow: off -> on` during a temporary parameter edit | `touchpostshow` is volatile COMSOL Desktop/result display state that can change merely through observation | Excluded the confirmed volatile display property from structured model-diff records and reran the one-change/zero-after-restore smoke test | Treat generic property diffs as evidence, filter only empirically confirmed volatile properties, and perform targeted queries before approving physics-affecting changes | `scripts/capture_comsol_model_snapshot.m`; `runs/20260717_171637_checkpoint_pause_resume_test/manual_change_report.md` |
| 2026-07-17 | Snapshot timestamps ended with `***`, and a COMSOL-generated M-file contained only a small subset of a live model | COMSOL 5.6/MATLAB R2022b did not render the requested `XXX` timezone pattern as intended; model history was disabled so M-file export did not reconstruct full current state | Switched to a validated timezone-neutral millisecond timestamp and marked undersized M-file export as supplementary/incomplete while keeping structured API snapshots primary | Test timestamp formats in the installed MATLAB; never treat an exported M-file as complete without inspecting it, especially when model history is disabled | `scripts/capture_comsol_model_snapshot.m`; `scripts/comsol_visual_checkpoint.m`; `runs/20260717_171637_checkpoint_pause_resume_test/snapshot_before_protocol_test_model_export.m` |
| 2026-07-18 | A successful saved-model reopen was complete, but a subsequent filesystem SHA-256 read reported that the `.mph` file was in use | The test model tag `StructuralCantilever` remained loaded in COMSOL Server and retained a file lock after the independent MATLAB MCP session ended | Queried `ModelUtil.tags()` and `ModelUtil.modelsUsedByOtherClients()`, confirmed the exact test tag was not used by another client, removed only that tag, and then computed the model hash successfully | After a test run, remove only the automation-owned exact model tag after checking other-client tags; never remove unrelated Server models merely to release a file lock | `runs/20260718_001618_structural_cantilever_new_skill`; `runs/20260718_001817_structural_reopen_verification` |
| 2026-07-18 | A symmetric permanent-magnet case produced a stable attractive force but a nonphysical lateral component above 10%, which decreased slowly under repeated tetrahedral refinement | The primary magnetic field was visually symmetric; the dominant error came from Maxwell-stress integration immediately outside sharp cuboid magnet faces on a non-symmetric surface mesh, not from the other magnetic sources | Preserved the solved model, rendered orthogonal field-line views with force overlays, isolated one source group at fixed geometry and mesh, then integrated Maxwell stress on several symmetric closed cuboids in air; the lateral ratio fell below 1% while the main force stayed stable | Do not blindly refine the whole mesh when a derived integral violates symmetry. Separate field convergence from integral convergence, visualize first, isolate one cause at a time, and cross-check sharp-edge surface forces with an auxiliary surface or energy method | `references/result_diagnostics.md`; `scripts/evaluate_maxwell_force_probe.m` |
| 2026-07-18 | A correct CAD coil measurement report incorrectly recorded `moduleNormalAgreement=false` | JSON decoded the normal as a column vector while the comparison constant was a row vector; implicit expansion produced a matrix before `norm` | Converted the decoded vector explicitly with `value(:).'` before comparison and reran the read-only measurement in a new run directory | Normalize vector orientation before scalar assertions or JSON status fields; never trust an aggregate boolean without inspecting the underlying measured vectors | `src/measure_stage2_coil_geometry.m`; `runs/20260718_141645_stage2_cad_coil_measurement` |
| 2026-07-18 | Rectangular-coil flux values oscillated nonmonotonically as a single global surface quadrature order increased | Some coil spanning surfaces crossed the passive magnet; `Bx` changed sharply at the magnet side faces, violating the smooth-integrand assumption of one tensor-product rule | Rendered three `Bx` coil planes with CAD and magnet overlays, then split x averaging and every y-z surface at the passive-magnet bounding-box faces before applying quadrature; adjacent final changes fell below 0.03% | Visualize material intersections before increasing quadrature; partition integrals at known field/material interfaces and test numerical convergence without changing the solved mesh | `src/evaluate_rectangular_coil_flux.m`; `runs/20260718_142141_stage2_theta0_flux_convergence`; `runs/20260718_142605_stage2_coil_flux_plane_diagnostic`; `runs/20260718_142830_stage2_theta0_flux_partitioned_retry1` |
| 2026-07-18 | A three-model MATLAB MCP batch wrote every result, saved the final `.mph`, and printed its PASS marker, but the MATLAB process then exited with Windows status `0xc0000374` (heap corruption) | The native-process failure occurred during teardown after sequentially opening and removing several large COMSOL models in one MATLAB batch; the exact low-level COMSOL/MATLAB defect was not isolated, while clean one-model sessions reproduced every result | Preserved the completed batch as evidence, then reopened and recomputed each non-baseline angle in a separate fresh MATLAB MCP session; the saved Stage 2 model reopened cleanly and all flux, force, and torque differences were below `2e-15` relative | For independent large `.mph` cases, prefer one model per MATLAB MCP batch. Never accept a PASS marker alone after an abnormal process exit; require a clean fresh-session reopen or recomputation before promoting the artifacts | `src/run_stage2_three_angle_pilot.m`; `src/verify_stage2_single_angle.m`; `runs/20260718_143518_stage2_three_angle_pilot`; `runs/20260718_144900_stage2_verify_theta7p5`; `runs/20260718_145200_stage2_verify_theta15` |
| 2026-07-19 | Imported CAD appeared at about 0.1 on axes labeled mm, while a later structural Shell was positioned near 100 mm and could not contact the retained 1 mm foam | `comp1/geom1` used mm but both Parasolid imports used `unit=current` with numerical CAD extents expressed near 0.1; early manifests then mislabeled `mphgetcoords` and `mphmeasure` outputs as SI, hiding a 1000-fold length mismatch | Used a controlled 10 mm cube in separate mm and m geometries to prove the installed COMSOL 5.6 unit semantics, then required the runtime-audited `importedgeomscale=1000` on both Parasolid imports together and rebuilt the Shell with unit-bearing mm parameters in a new mph copy | Never label LiveLink geometry coordinates, areas, or volumes without recording `geom.lengthUnit()` and `geom.geomScaleFactor()`. Before physics, compare at least three known CAD dimensions and one volume against their unit-consistent targets; scale all same-assembly imports together | `src/audit_comsol_length_unit_semantics.m`; `docs/stage4_recovery_and_alignment_plan.md`; `runs/20260719_030719_comsol56_unit_semantics_audit_retry1` |
| 2026-07-19 | A scratch geometry accepted `PartitionDomains` but rejected the assumed `input`/`tool` selections, and a later domain-count assertion failed when evaluated before geometry finalization | `PartitionDomains` uses the installed properties `domain` and `workplane`; the object-level `Partition` feature uses `input`, `partitionwith=workplane`, and `workplane`. Finalized entity IDs/counts are not available after only `geom.run(featureTag)` | Queried installed feature properties, checked the COMSOL 5.6 programming reference, then verified object-level `Partition` on a 10 mm cube and called full `geom.run` before asserting two domains | Do not transfer selection-property names between `Partition`, `PartitionDomains`, and `PartitionFaces`. Audit the installed properties and count finalized entities only after full geometry finalization | `src/audit_comsol56_partition_geometry_api.m`; `runs/20260719_143901_comsol56_partition_geometry_api_audit`; `runs/20260719_144051_comsol56_partition_geometry_api_audit`; `runs/20260719_144145_comsol56_partition_geometry_api_audit` |
| 2026-07-19 | A verified one-face flat Shell failed an absolute area equality assertion although its coordinate spans were exact | COMSOL 5.6 `mphmeasure` returned a surface area 0.000128337 mm^2 below the analytical 3904.9119325365 mm^2 rectangle, a relative difference of only 3.29e-8; the absolute 1e-6 mm^2 tolerance was unnecessarily strict | Preserved the failure, independently reopened the saved model, confirmed one selected face and exact bounding spans, then changed the check to record the delta and require relative error below 1e-6 | Validate geometry with topology, coordinates, and a scale-aware relative measurement tolerance; never weaken a failed assertion before a read-only audit proves the selected entity and discrepancy | `src/build_stage4_continuous_shell_geometry_checkpoint.m`; `runs/20260719_165206_stage4_continuous_flat_shell_geometry_checkpoint`; `runs/20260719_165415_stage4_flat_shell_area_audit` |
| 2026-07-19 | A generated fresh-reopen launcher stopped at MATLAB parse time before creating its run directory | One nested `sum(counts(strcmpi(...)))` expression was missing a closing parenthesis | Corrected the launcher expression and reran the same read-only checks in a new named verification attempt | Keep long generated launchers syntactically shallow; parse-sensitive calculations should be split into separate assignments before execution | MATLAB MCP launcher `codex_s4_contmesh_verify_170420`; `runs/20260719_170650_stage4_continuous_contact_mesh_fresh_reopen_retry1` |
| 2026-07-19 | Fresh-reopen validation failed on `model.getVersion()` and `collection.contains(tag)` | Those assumed Java methods were not valid for the returned COMSOL 5.6 client objects | Removed the unnecessary version call and converted `model.study.tags()`/`model.sol.tags()` with the verified Java-tag helper before `strcmp` | Never infer Java collection conveniences from MATLAB types; use `tags()` plus `cell`/`cellfun(@char,...)` and query installed methods when needed | `runs/20260719_180500_stage4_contact_approach_chunk1_fresh_reopen`; `runs/20260719_180600_stage4_contact_approach_chunk1_fresh_reopen_retry1` |
| 2026-07-19 | Reading Study `plistarr` with `getString` failed during a prepared-model verification | `plistarr` is not a scalar string property in this COMSOL 5.6 feature | Verified the persisted scalar initialization properties and target from the run-local JSON; use the appropriate array getter only after querying the property | Do not force array-valued COMSOL properties through `getString`; avoid nonessential display-field assertions | `runs/20260719_180900_stage4_continuous_contact_checkpoint_0p885_prepare_fresh_reopen` |
| 2026-07-19 | A refined plot showed coordinates 1000 times too large although displacement and pressure were correct | `mpheval(...).p` already used the geometry coordinate unit mm; the plot multiplied it by 1000 again | Preserved the incorrect image, asserted expected coordinate ranges, and generated a corrected visualization without scaling `p` | Treat coordinate units independently from expression units and assert physical coordinate ranges before exporting plots | `runs/20260719_181100_stage4_continuous_contact_checkpoint_0p885`; `runs/20260719_181400_stage4_continuous_contact_checkpoint_0p885_corrected_visual` |
| 2026-07-19 | Strict `contact pressure > 0` area reported about 1913 mm2 at first contact while pressure was localized to narrow edge regions | Tiny positive interpolation/solver noise over much of the face polluted the zero-threshold indicator integral | Fresh-reopen audited areas above 0, 1, 5, and 10 Pa; retained the user's strict definition but used threshold sensitivity for effective area | Never report zero-threshold contact area alone; include force, peak pressure, plots, and several small positive thresholds | `runs/20260719_182300_stage4_contact_0p89_fresh_reopen_threshold_audit` |
| 2026-07-19 | Stationary paths 0.895 and 0.90 both produced Shell displacement/rotation NaN/Inf after path 0.89 first contact | The continuous plate lost the usable stationary post-contact branch; one midpoint did not cure it and failures alternated between physical free-side regions | Preserved both failures, stopped bisection and mesh changes, and switched to an API-evidenced transient-contact investigation | After first-contact failure, attempt at most one midpoint; if the failure class repeats, diagnose branch stability instead of repeatedly shrinking steps | `runs/20260719_182500_stage4_continuous_contact_adaptive_0p90`; `runs/20260719_183100_stage4_continuous_contact_recovery_0p895` |
| 2026-07-19 | The first Time Dependent run stopped before integration while merging stationary initial values | Changing `AugmentedLagrange` to `AugmentedLagrangeDynamic` changed the contact auxiliary-variable set, so the verified stationary seed could not populate all transient variables | Used the COMSOL 5.6 official `transient_rolling_contact.mph` as evidence to retain `AugmentedLagrange` for the retry; recorded that allowed values do not prove seed compatibility | Before stationary-to-transient reuse, compare dependent variables and preserve the contact method or explicitly map compatible fields | `runs/20260719_190000_stage4_dynamic_contact_relaxation`; `runs/20260719_184500_comsol56_official_transient_contact_api_audit` |
| 2026-07-19 | A compatible `AugmentedLagrange` transient still spent about 20 minutes and failed with no accepted time above zero | The auto-generated generalized-alpha solver used free stepping with a nominal `0.01 s` step, about 24 times the requested output interval; the installed COMSOL 5.6 transient-contact examples instead use manual generalized-alpha steps | Compared all readable Time-solver properties against the official rolling-contact model, kept `rhoinf`, predictor, tolerance, and consistent initialization unchanged, and set a bounded manual step equal to one quarter of the output interval; a fresh-reopen-verified prefix advanced through three nonzero steps | When a transient fails at `t=0`, audit stored time values and compare solver properties with an official same-physics model before changing tolerance or mesh. Validate a short prefix before a longer run | `runs/20260719_191000_stage4_dynamic_contact_relaxation_retry1`; `runs/20260719_193759_stage4_dynamic_initialization_diagnostic_retry2` |
| 2026-07-19 | A five-state diagnostic assertion found only four stored times although the solver had no error | The decimalized range endpoint was one floating-point increment below four decimalized manual steps, so COMSOL correctly excluded the nominal fifth value | Accepted the four finite states without rerunning, independently reopened and reproduced them, then defined future endpoints as integer multiples of the manual step in the JSON parameter source | Build deterministic diagnostic time lists from integer multiples of one parameterized step; do not treat an endpoint-rounding state-count mismatch as a physics failure before inspecting `mphsolinfo.solvals` and the solver error message | `runs/20260719_193759_stage4_dynamic_initialization_diagnostic_retry2` |
| 2026-07-19 | MATLAB was terminated to stop a long solve, but COMSOL Server continued calculating and Desktop later displayed the still-running convergence state | The controlling MATLAB client was stopped after the solve had already been submitted to Server; controller exit was incorrectly treated as solver cancellation | Terminated only the exact recorded Server PID after explicit user authorization, preserved the prepared model, and required independent stop verification | Never kill MATLAB alone as a stop procedure; provide `progress.log`, convergence plots, checkpoints, and verify Server/solve state before reporting `STOP_CONFIRMED` | `references/progress_and_convergence_monitoring.md`; `runs/20260719_194651_stage4_dynamic_static_limit_crossing` |
| 2026-07-19 | MATLAB MCP timed out at 300 s during a monitored transient, but COMSOL Server continued and completed the same solve | MCP transport duration was shorter than the bounded nonlinear solve; timeout was not a solver failure | Read the same run's `progress.log`, process CPU, diary, and artifacts without resubmitting; accepted only after solved MPH creation and fresh-reopen verification | A timed-out MCP call never authorizes a retry while the recorded Server solve is active; use progress logging and artifact completion evidence | `runs/20260719_232655_stage4_dynamic_observable_prefix_block2` |
| 2026-07-19 | Stage 4 spatial postprocessing first rejected `mpheval` dimension syntax and then MATLAB `trisurf` rejected the COMSOL connectivity | COMSOL 5.6 requires `mpheval(...,'edim','boundary',...)` for Shell faces, and `mpheval.t` returned zero-based `int32` connectivity | Used the allowed dimension from the installed error message, audited `p/t/d` sizes and index range, then converted triangles with `double(t)+1` | Query installed allowed values and audit connectivity range before plotting; do not assume `surface` or one-based indices | `src/render_stage4_dynamic_block2_contact_states.m`; `runs/20260719_232655_stage4_dynamic_observable_prefix_block2` |
| 2026-07-20 | Stage 4C C0 stopped while asserting `pair.getType()` and later while assigning mesh evidence into a preallocated empty MATLAB structure | COMSOL 5.6 `PairClient` does not expose `getType()` through this LiveLink client, and MATLAB does not permit assignment of a populated structure into a zero-field structure array | Retained the source creation provenance plus source/destination entity queries for the Contact pair; collected per-mesh structures in cells and concatenated them only after all meshes were built | Do not assume every COMSOL client object has `getType()`; validate a pair through its creation provenance, referenced ShellContact feature, and source/destination queries. Use cell collection for structures whose nested cell-valued fields are assembled in a loop | `src/build_stage4c_controlled_indentation_c0.m`; `runs/20260720_003917_stage4c_c0_controlled_indentation`; `runs/20260720_004103_stage4c_c0_controlled_indentation_retry1` |
| 2026-07-20 | Stage 4C model and all meshes saved, but `mphlaunch` returned without the exact model tag appearing in `modelsUsedByOtherClients()` | The Desktop client did not remain attached; no evidence supports treating the launch return as synchronization | Marked visual synchronization unverified, kept `SAFE_TO_EDIT=false`, and independently reopened the saved model through a fresh MATLAB MCP session without solving | `mphlaunch` return is never sufficient; require the exact other-client tag. A valid no-solve model checkpoint may be accepted separately from visual synchronization, but GUI editing remains prohibited | `runs/20260720_004338_stage4c_c0_controlled_indentation_retry3`; `runs/20260720_004639_stage4c_c0_fresh_reopen_verification` |
| 2026-07-20 | The first Stage 4C C1 solve failed at compile time with undefined Shell displacement variable `u` | COMSOL allocated the continuous-Shell displacement field as `u2,v2,w2`; the unqualified conventional name `u` belonged to no active field on the selected boundary | Queried `shell.field('displacement').component()` from the installed model, rebuilt the weighted-average expression with its returned first component, and preserved the failed MPH and error report | Never hardcode structural dependent-variable names when a model contains several physics histories; query the active physics field components and assert the persisted expression before solving | `runs/20260720_005652_stage4c_c1_coarse_indentation`; `runs/20260720_010049_stage4c_c1_coarse_indentation_retry1` |
| 2026-07-20 | All six authorized C1 indentation points converged but reported zero contact | This was not a solver or postprocessing failure: fresh reopen reproduced zero FN/area/pressure, the monitored midspan gaps remained about 2.76 to 2.57 mm, and the global finite minimum gap remained positive at 0.1531 to 0.1523 mm | Accepted the numerical execution but classified the contact-spread question as unanswered; stopped before medium/fine meshes or range extension | A mesh comparison is meaningless when the coarse baseline never reaches the target physical event. Verify the reference configuration and local gap trajectory before spending on additional meshes | `runs/20260720_010049_stage4c_c1_coarse_indentation_retry1`; `runs/20260720_010521_stage4c_c1_fresh_reopen` |
